#!/bin/bash

sudo apt update
sudo apt install python3 python3-pip docker docker-compose 
sudo apt install libgconf-2-4 #for postman
sudo apt install gnome-keyring #for mysql workbench
pip3 install --upgrade docker docker-compose
